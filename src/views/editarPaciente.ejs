<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Paciente</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <%- include('partials/header', { title: 'Editar Paciente'}) %> 
    
    <div class="container">

    <h1>Editar Paciente</h1>

    <form action="/usuarios/pacientes/editar" method="POST" class="form-control">
        <!-- Hidden input to store the patient ID -->
        <input type="hidden" name="id" value="<%= paciente.id %>">

        <label for="nome">Nome:</label>
        <input type="text" id="name" name="name" value="<%= paciente.name %>" required>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= paciente.email %>" required>

        <label for="telefone">Telefone:</label>
        <input type="tel" id="telefone" name="telefone" value="<%= paciente.telefone %>" required>

        <button type="submit" onclick="this.disabled=true;this.form.submit();">Editar</button>

         <button class="btn"><a href="/usuarios/pacientes" class="btn">Voltar</a></button>
        </form>
       
      </div>
  <%- include('partials/footer', { title: 'Editar Paciente' }) %>
</body>
</html>